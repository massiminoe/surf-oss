/target/
**/*.rs.bk
.DS_Store
.idea/
.vscode/
*.swp

# Agent / LLM working notes — keep local, out of history for now
/CLAUDE.md
/AGENTS.md
/docs/
.playwright-mcp/

# Local Python tooling
.venv_ai/

# Third-party replay binaries (local cache only; not for redistribution)
/assets/replays/external/

# Map BSPs — 17 MB–175 MB each, ~1.4 GB for the corpus. Not committed.
# Reproducible via `python3 tools/fetch_maps.py --all`, pinned by sha256 in
# assets/maps/manifest.json (which IS tracked).
/assets/maps/*.bsp
/assets/maps/*.bsp.part

# Local Python bytecode
tools/__pycache__/

# Ad-hoc local screenshots / inspect dumps (keep fixtures elsewhere)
/*.png

# Locally selected CS:S installation and retained setup scratch downloads
/assets/game-dir.txt
/assets/**/*.part

/assets/downloads/
